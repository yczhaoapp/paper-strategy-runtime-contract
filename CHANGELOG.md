# 2.8.3 — 2026-09-23

- Backtrader 账户快照按原生成交累计毛已实现盈亏，并以当次可用收盘价计算未实现盈亏；盈利、亏损、平仓和反向持仓均有数值回归，不再让默认零值伪装真实账户状态。
- 严格容器验收启动即使旧成功收据失效；每次尝试单独暂存，构建、镜像检查、运行或出版失败均写入本次失败收据，成功时才发布同一 `attempt_id` 的镜像、验证和验收文件。
- 证据发布器拒绝混合不同严格验收尝试的收据，并在写入发布文件前完成三方一致性核对。

# 2.8.2 — 2026-09-23

- 策略动作、训练制品清单和包内 manifest 在资源围栏仍生效时跨越 JSON 边界并按契约类型重建；宿主组件不再接收策略构造的延迟容器、标量子类或嵌套可执行对象。
- 增加动作延迟迭代器与制品 `str` 子类回归；两条反例进入正式验收门禁，覆盖推理和训练返回边界。
- 更新安全边界与交付文档，统一描述“围栏内消费、围栏外只使用规范对象”的规则。

# 2.8.1 — 2026-09-23

- 策略训练回调不再接收主机 `ArtifactStore`；每次运行使用独立的最小 `ArtifactIO` 能力对象，存储根、验证方法和服务类留在受信侧。
- 能力通道在进入受信 I/O 前只接受内建标量、字节和字符串字典；类修改与带回调的 `str` 子类不能改变授权路径或在审计豁免区执行策略代码。
- `load` 回调必须实际读取主机已验证的模型字节；空加载、缺失模型、伪造 manifest 和能力类方法替换均结构化失败，不能生成成功 Bundle。
- `ACCEPTANCE_MATRIX.yaml` 与实际保真范围统一为至少 14 个复现、最多 4 个方法适配；验收器严格解析矩阵阈值和深度论文 ID，更严格的 15/3 反例必须失败。
- 三类通用外部接入测试统一称为“作者夹具”，不再表述为未知真实论文复现；安全文档区分进程内资源围栏和严格容器宿主隔离。
- Docker 运行使用随机容器名；客户端超时后显式强制清理，并在结构化错误中记录清理结果。

# 2.8.0 — 2026-09-23

- 训练产物由受信运行时在训练后和加载后独立重验；存储根不可由策略改写，缺失文件、伪造大小、越界和符号链接均结构化失败。
- manifest 描述符和策略方法解析进入资源围栏；公共 Adapter 对实际字段、L2 深度、异常出口、重复目标和目标派生订单限制统一执行。
- Reference 与 Backtrader 对目标仓位派生数量、待处理订单和最终成交再次执行限制；迟到 bar 按市场时间聚合 OHLC，重复市场时间明确拒绝。
- Ridge 使用训练与推理一致的三个因果滞后收益；L2 成交概率限定为 θ=0 常数死亡率特例，按计数/暴露时间估计强度并计算 500ms 有限期限概率。
- 论文绑定升级到 1.2，引入 A0 方法适配。Spooner 三个算法样例和 A2C Pairs 如实从 A1 降为 A0；18 个样例中 14 个计为复现、4 个计为适配，不以配额覆盖语义偏差。
- 新增独立审计反例验收项，并要求本轮 JUnit 实际执行产物、描述符、Adapter、订单、重采样和论文声明修复测试。

# 2.7.4 — 2026-09-22

- 所有已提交的 Schema 和策略包生成器显式写入 LF，避免 Windows 文本流把临时生成物转换为 CRLF 后误报生成物漂移。
- GitHub Actions 的 push 触发限定为 `main`，发布标签复用已通过的提交，不再重复占用四个平台 runner。
- 修复来自 v2.7.3 Windows 真机已通过来源获取、哈希、Ruff、mypy、Schema 和 package 生成后的下一层失败；v2.7.3 标签保持不可变。

# 2.7.3 — 2026-09-22

- 固定公开 CSV 为逐字节文件，不再让 Git 的文本规范化把审阅过的 CRLF 来源转换成 LF；干净克隆中的实际 SHA-256 与来源登记保持一致。
- 所有可移植文本显式检出为 LF，公开数据许可证保持原始字节，使 Windows、Linux、macOS 和严格容器能够重算同一个输入树哈希。
- 该修复来自新公开仓库的首轮四平台 CI，而不是本地已有工作树；v2.7.2 标签保持不可变，修复发布为 v2.7.3。

# 2.7.2 — 2026-09-22

- Tabular Q、SARSA、Double Q 新增完整黑盒差分 oracle：每种算法动态生成 16 份至少 9 步的训练请求，由测试侧独立实现全部训练轮次并逐项核对持久化 Q 表。
- 差分 oracle 同时核对训练请求哈希、数据集和 seed，并在重新加载制品后通过公开 `on_event` 入口验证训练集中未见状态的最近状态决策。
- 三个测试进入论文 claim 绑定和硬验收，和原有参数化、性质、64 组随机单步公式及端到端回测共同构成多层证据。

# 2.7.1 — 2026-09-22

- Tabular Q、SARSA、Double Q 新增 17 个多层 oracle 节点：多参数、terminal、零学习率、非贪心实际动作、双表交换对称性，以及每种算法 64 组固定 seed 随机样本。
- 预期值全部由测试侧独立论文公式计算；生产实现不参与 expected 计算，并增加 AST 回归以禁止测试、数据集、seed 或哈希驱动的生产分支。
- 三个 RL 论文绑定和硬验收直接要求新 oracle，同时保留训练、制品保存、重载、推理、下单、回测生命周期证明；忠实度继续保持 A1。

# 2.7.0 — 2026-09-22

- Runtime Contract 升级到 1.4：新增 `RuntimeCapabilities`，把 `training.supervised.v1` / `training.rl.v1` 与引擎的数据、动作、执行画像分开协商，并在执行计划与 RunBundle 中固定提供者和哈希。
- Backtrader 增加监督 Logistic 与 RL Tabular-Q 的训练、保存、重载、推理、回测全生命周期门禁，不再只验证规则型 bar 策略。
- 策略包只开放 `psrc.strategy_api`；静态扫描解析 import 别名和完整属性路径，运行时审计钩子阻止直接文件、进程、网络访问，只有受信 `ArtifactStore` 可写产物。
- Tabular Q、SARSA 与 Double Q 增加专用数值 oracle 并接入论文声明测试绑定；鉴于状态、动作和实验环境仍有差异，三者继续保持 A1，未虚报 A2。
- 严格容器构建默认使用 `--no-cache --pull`，并记录镜像内容 ID。

# 2.6.3 — 2026-09-22

- Runtime Contract 升级到 1.3：`BacktestAdapter.run` 成为统一的已验证模板入口；直接调用也会绑定计划、策略、引擎能力、沙箱和源事件，并执行声明的兼容转换后再进入引擎钩子。
- `run_trainable` 在训练开始前重算实际 `TrainingInputEvidence` 并与执行计划比较，训练载荷漂移以 `TRAINING_DATA_MISMATCH` 失败且不产生模型。
- `ArtifactManifest` 增加训练请求规范哈希；orchestrator 和 `RunBundle` 同时核对请求哈希、训练数据集与 seed，模型产物不能再借用另一份训练证据。

# 2.6.2 — 2026-09-22

- Runtime Contract 升级到 1.2：`ExecutionPlan` 固定策略数据要求和最低沙箱，Adapter 实例公开稳定能力声明；orchestrator 在生命周期开始前强制核对实际策略、引擎能力和沙箱等级。
- `RunReport` 与 `RunBundle` 增加上下文防御性验证，计划要求严格容器而实际报告开发模式时不能构造成功证据。
- 执行 `DataRequirement.lookback` 和 `max_staleness_ns`：编译期拒绝必然不足的记录数，兼容转换后逐标的核对观察数，并以 `available_time - event_time` 执行陈旧度上限。
- 补齐 OHLCV 特征顺序、L1 队列不平衡公式和 Actor–Critic 推理权重边界的独立 oracle；线性 Actor–Critic 按实际差异由 A2 降为 A1，项目保持 9 个 A2。
- 移除未被策略绑定或深度案例使用的 DeepLOB 来源登记；发布集固定 16 份原文，其中 15 个来源覆盖 18 个策略。

# 2.6.1 — 2026-09-22

- Reference 对直接订单执行累计仓位约束：接单、改单和成交前都核对当前持仓与待成交订单，不能用多笔合法小单绕过 `max_abs_position`。
- `day` 订单增加明确的 UTC/24×7 会话边界，在下一 UTC 日期的第一条事件撮合前过期；其他日历或时区不再被猜测执行。
- 实际 bar 的 `event_time` 必须落在声明的 UTC/epoch 周期边界；允许休市、周末和缺 bar 形成的整数倍间隔。
- 合约版本字段增加 SemVer Schema 约束，编译器对绕过模型验证的无效版本仍返回结构化错误。
- 复用运行输出目录时先清理 PSRC 自有产物；失败运行不会与旧成功 Bundle、成交或模型文件混存，未知用户文件保持不变。
- 为 SMA、Donchian、Pairs Z-Score 和 TWAP 补充独立公式/边界 oracle，并把 A2 claim 强制连接到独立复现测试；Logistic oracle 增加手算的一步梯度断言。

# 2.6.0 — 2026-09-22

- 新增 `psrc author run` 外部论文准入：调用方提供的 PDF/HTML/文本、页内证据、结构化规格、manifest 和策略源码在导入前完成哈希绑定；规则、监督和 RL 作者夹具均有黑盒执行测试。
- 收紧公共行情种类为实际可执行的 OHLCV、trade、L1 quote 和 L2 snapshot；补充 trade tick、账户现金/权益/持仓、挂单 accepted/replaced 状态及成交的端到端测试。
- 增加外部准入 Schema 和公开接口覆盖文档，Schema 总数增至 31；验收要求上述接口测试和三类作者夹具测试来自本轮 JUnit。夹具只证明通用接入形状，不计为额外真实论文复现。
- 为 12 个可训练策略包增加 `TrainingInputEvidence`，把完整训练请求绑定到执行计划和 RunBundle；训练数据标识或载荷漂移在训练前失败。
- 验收报告改用证据根目录相对路径，并发布与当前输入树哈希一致的可移植宿主/严格容器收据。
- Docker 构建上下文排除本机论文缓存，镜像必须重新获取并校验 17 份注册原文；延长依赖下载超时并增加有限重试。
- 统一发布元数据、版权声明和审阅文档，形成独立项目版本。
- 在所有默认适配器入口统一执行动作声明和风险限制校验。
- 将实际事件与数据声明绑定，校验 payload kind、字段、标的、记录数、L2 深度和内容哈希；含糊多流输入失败关闭。
- Reference 对改单重新校验数量上限，并明确拒绝尚未实现的非 `day` 有效期。
- 统一线性 Actor–Critic 训练/推理的组合权重语义，增加 A2C 与连续 Actor–Critic 独立一步更新 oracle。
- 将固定 1:-2 spread 的 A2C Pairs 从 A2 降为 A1，避免证据等级超过实现。
- 普通 `psrc run` 失败统一落盘 `FailureReport`，保留可取得的策略、数据、引擎和实际输入上下文。
- 使用仓库无关的 Schema URN，统一 PSRC 品牌和文档口径，并建立可独立发布的本地 Git 版本。

# 2.5.0 — 2026-09-21

- 将 D1 硬门槛从 3 个提高到 6 个，规则、监督、RL 各至少 2 个；新增 Pairs Z-Score、Ridge Return 和 A2C Pairs 的公开历史数据运行。
- 固定 Plotly `stockdata.csv` 的 AAPL/MSFT 2306 日公开样例、来源记录、许可证和文件哈希；双资产事件按日期同步并延迟到下一 UTC 日可用。
- Ridge 与 A2C Pairs 使用前 400 日训练、后 106 日测试；训练数据 ID、来源哈希、特征/状态、奖励和偏差进入可审计证据。
- 策略卡不再把全部策略笼统写成合成数据演示，避免与 6 个 D1 包的数据证据冲突。

# 2.4.0 — 2026-09-21

- 将规则 Donchian、监督 Logistic 和 RL Actor–Critic 提升到 D1 公开替代数据；三类各至少一个策略使用固定哈希的 AAPL 历史行情，训练/测试按 400/106 根日线因果切分。
- 公开数据绑定现在强制核对 `DataSourceEvidence`、事件来源、训练数据 ID 与元数据；验收新增 D1 总数和分类型门槛，同时固定全部 18 个策略为 E0，禁止无证据的实证结果升级。
- 在 Linux/arm64 Docker Desktop 中完成 2.4 严格验证；18 个运行包均由断网、只读根目录、非 root、零 capabilities、`NoNewPrivs` 和资源限制环境生成。
- 修复 NumPy/BLAS 在 Python 3.12 与 3.13 间的浮点尾差：训练产物和预测按 12 位规范化后再内容寻址，使宿主与严格容器行为签名一致。
- 容器启动器与单策略沙箱可自动发现 macOS Docker Desktop 内置 CLI，不再依赖全局 PATH。
- 严格镜像纳入 `.github`、`.gitattributes` 和 `NOTICE` 等全部审阅输入；宿主与容器收据现在必须绑定同一输入树哈希，并有回归测试防止再次遗漏。

# 2.3.0 — 2026-09-21

- 将 18 个策略全部提升为公式、算法或等价方法复现，删除方法适配等级；来源覆盖从 11 个提高到 15 个，注册表固定 17 份原文。
- 新增算法、数据、实验和运行时四轴保真模型；11 个策略达到 A2 算法精确级，三类策略分别至少 2 个，所有契约示例继续明确标为 D0/E0/R1。
- L1 微价格、Avellaneda–Stoikov 做市、OHLCV Logistic、队列成交概率、队列不平衡 MLE、A2C 配对、连续 Actor–Critic 等实现改为论文公式或训练更新，并增加 9 个独立数学 oracle。
- 用风险厌恶上下文 bandit 替换泛化执行 bandit，用 A2C 配对替换 Dyna-Q 配对；刷新 18 个策略包及确定性运行行为签名。
- 将论文验收门槛提高到 15 个来源、6 个公式复现、18/18 复现、至少 9 个 A2、每类至少 2 个 A2、零方法适配。

# 2.2.0 — 2026-09-21

- 将论文绑定契约升级到 1.1：每条 claim 必须连接实现符号和精确 pytest 节点。
- 18 个策略新增确定性行为签名，绑定输入与决策哈希、实际动作/原因码、训练产物及决策/订单/成交数量。
- 论文覆盖门槛提高到至少 10 个独立来源、至少 9 个公式/方法复现、每类至少 1 个高忠实度实现，方法适配最多 9 个。
- 总验收交叉核对当次 JUnit 和新生成 RunBundle；未执行测试或任一行为漂移均失败，并增加篡改反例。
- 新增 `PublicDatasetSource` Schema；公开 CSV、来源登记和许可证文本分别固定哈希。Schema 总数增至 29。

# 2.1.0 — 2026-09-21

- 18 个契约策略现在全部有一对一论文绑定：来源哈希、页级声明、代码哈希、验证测试、假设与偏差。
- 新增 12 项公开论文来源注册表；验收要求三类各 6 个绑定、至少 8 个不同来源，并独立重算全部证据。
- 将论文落地分成公式复现、方法复现和方法适配，禁止把适配项写成逐公式复现。
- Ma 深度案例改用固定哈希的公开 AAPL 2015–2017 OHLCV，并新增数据来源证据 Schema 和前视防护测试。
- 新增题目模糊项解释登记表和严格验收政策；Schema 总数增至 28。

# 2.0.0 — 2026-09-21

- 完成 Runtime Contract 1.1.0 与 Paper Pipeline 1.0 的整体设计。
- 4 个真实 PDF 论文的证据、算法编译、统一运行和独立重验；其中日线 VMA 进入 Backtrader。
- 新增论文/配方/复现规格 Schema；总计 25 个。
- 修复 Windows 身份接口、长路径和产物原子保存；容器断网执行不再触发 uv 构建。
- Python 跨平台门禁及三平台 CI；Nautilus 从默认依赖拆成可选 extra，默认两引擎动态验证。
- 新增数学 oracle、标签泄漏、训练失败、来源/代码篡改、沙箱拒绝等回归测试。

## 早期版本

# 变更记录

本项目遵循语义化版本。Contract 字段和机器标识符保持英文，评审说明采用中文。

## Unreleased

- JSON Schema 改为自包含 Draft 2020-12 文档，并用标准验证器对 22 类真实文档执行无网络验证。
- 统一策略目录入口增加显式 `--engine`，可直接选择 Reference、Backtrader 或 NautilusTrader，禁止隐式替换。
- 增加规则、监督学习和强化学习三类外部策略包的统一入口端到端验证。

## 1.1.0 — 2026-09-03

- 为 `ExecutionPlan` 和 `RunBundle` 增加可选策略源码证据，绑定策略包源码与受信运行时源码树；当前完整验证要求新生成的运行证据包含该文件。
- 增加显式、无损、可逆的 `symbol.map.v1`，并证明它可与需授权的 `bar.resample.v1` 组合执行。
- 明确 `StrategyManifest.resources.sandbox` 是最低隔离要求；运行策略试图降低该要求时返回 `SANDBOX_POLICY_DOWNGRADE`。
- 增加 `psrc sandbox run`，把单个外部策略包实际送入无网络、只读根文件系统、非 root 的 Docker 边界。
- 增加 `psrc author audit`，提供论文规格到策略 manifest 的确定性、机器可读审计入口。
- 增加 Contract `1.0.0` 文档向后解析回归测试；本次仅新增可选字段、错误码和转换标识符，因此保持 major 版本不变。

## 1.0.0 — 2026-09-02

- 首次公开稳定 Contract、18 个策略包、三套可执行引擎、结构化失败证据及严格容器验证。
